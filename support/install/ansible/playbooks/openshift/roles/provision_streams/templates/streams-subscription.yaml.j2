apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: amq-streams
  namespace: '{{ operators_project }}'
spec:
  channel: stable
  installPlanApproval: Automatic
  name: amq-streams
  source: messaging-streams
  sourceNamespace: '{{ operators_project }}'
  startingCSV: 'amqstreams.v{{ streams_version }}'